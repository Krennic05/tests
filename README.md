Para el proyecto utilice las siguientes instalaciones en consola:

npm install node, 
npm install mysql2, 
npm install express, 

npm install bcrypt,
npm install bcrypt express-session,

para iniciar el servidor ejecutar en consola node server.js
